let v = function (x) {
    return x*2
}

console.log(v(2))

// variável pode receber funções em JS, pois o JS é uma linguagem funcional
// existem outras maneiras (sintax) de criar funções, com seta "=>" por exemplo